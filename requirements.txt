pillow>=10
requests>=2.31
winsdk>=1.0
# 音訊字幕模式（run.bat 預設一起裝；缺任何一個字幕模式都會降級，所以不再列為選用）
pyaudiowpatch>=0.2.12          # 擷取系統聲音（WASAPI loopback）
onnxruntime>=1.17              # Silero VAD：分辨人聲與音樂，沒有它幻聽字幕會多很多
numpy>=1.24
opencc-python-reimplemented>=0.1.7   # 本地 LLM 譯文的簡→繁保底轉換
# 選用：Claude 引擎
# anthropic>=1.0
